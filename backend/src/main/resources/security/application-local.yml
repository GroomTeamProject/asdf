server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: ${DB_URL_LOCAL}
    username: ${DB_USERNAME_LOCAL}
    password: ${DB_PASSWORD_LOCAL}
  jpa:
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.MariaDBDialect
  jwt:
    secret: ${JWT_SECRET:1c8UWNf/DwyesyNfLKwiR/M76kryQySEoaYrzQGWyiWHJ1DzqsGJM323NQRmliZnOmFfwj1yahSq1vhmm78mDg==}  # 환경변수에서 불러오고, 없으면 default 값 사용
  flyway:
    enabled: false # 수동으로 Bean을 등록하기 위해 비활성화

cors:
  allowed-origins: ${ORIGIN_URL_LOCAL}

<<<<<<< HEAD
=======
cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    s3:
      bucket: ${AWS_S3_BUCKET}
    region:
      static: ${AWS_REGION}
    stack:
      auto: false
>>>>>>> develop
